Copilot Prompt Builder - App (3) standalone HTML package

Files:
- index.html: standalone HTML app with all JavaScript and CSS inlined.
- favicon.ico: optional browser icon.

Upload index.html to SharePoint or any static web server, or open it locally in a browser.

This package was built from app (3).zip's compiled dist output to preserve the Power App colors, layout, and features, including industry/sub-vertical targeting, role-aware context and expectation suggestions, disclaimers, placeholder editing, copy behavior, Copilot launch, and GCSE progress.
